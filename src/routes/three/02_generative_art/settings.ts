export const settings = {
    antialias: true,
    dimensions: [1024, 1280],
    // Get a WebGL canvas rather than 2D
    context: 'webgl',
    // Turn on MSAA
    attributes: { antialias: true },
    // ============================================================
    // New settings
    // ============================================================
    orbit: true
};
